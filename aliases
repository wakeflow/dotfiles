alias '$'=''
alias '..'='cd ..'

# python
alias python=python3
alias jn="jupyter notebook"

# Node
alias nm=nodemon
alias ccc='node /Users/andreaskater/Dev/ccc/ccc.js'
alias aaa='node /Users/andreaskater/Dev/ccc/aaa.js'
alias comp='$SCRIPTS_PATH/createComponent.js'
alias js='$SCRIPTS_PATH/createJsFile.js'

# General
alias x=clear
alias c=clear
alias cc=clear
alias copy=pbcopy
alias ls='ls -1a'

# Feathersjs
alias fgs='feathers generate service'

# Terraform
alias tf=terraform

# Docker
alias dcu='docker compose up'
alias dcd='docker compose down'

#Tree
alias t=tree
alias tree="tree -I 'node_modules|venv'"

#VIM
alias v=vim

#ZSH
alias relaod=". ~/.zshrc && echo 'ZSH config reloaded from ~/.zshrc'"
alias reload=". ~/.zshrc && echo 'ZSH config reloaded from ~/.zshrc'"

#zero44
alias dbpw='aws rds generate-db-auth-token --hostname "zero44-staging.czpz0cmcraqt.eu-central-1.rds.amazonaws.com" --port "5432" --region eu-central-1 --username "zero44_staging_readonly" | pbcopy'
alias om="overmind start -f Procfile.nginx.dev"

#revcontent
alias brc="bit create react --env envs/apex-env --scope revcontent.apex"
alias brctoken="export CODEARTIFACT_AUTH_TOKEN=$(aws codeartifact get-authorization-token --domain codeartifact-default --query authorizationToken --output text --profile revcontent --region us-east-1)"
